# Base URL of the Verilex backend API. During local dev the Vite dev server
# proxies /api to this target (see vite.config.js), so this is mainly used
# for production builds served separately from the backend.
VITE_API_BASE_URL=http://localhost:4000
